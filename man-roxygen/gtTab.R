#' @param gtTab A logical input indicating whether to generate the table(s) in
#'  `gt::gt()` table object format (`TRUE`). By default (`FALSE`), the table(s)
#'  are generated in `tibble::tibble()` format. Users can also apply the
#'  `writeTo` argument if they wish to export the `gt::gt()` table object(s).
