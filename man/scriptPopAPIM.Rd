% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scriptPopAPIM.R
\name{scriptPopAPIM}
\alias{scriptPopAPIM}
\title{Population lavaan syntax for a latent indistinguishable APIM}
\usage{
scriptPopAPIM(dvn, lvxname, lvyname, pop_values)
}
\arguments{
\item{dvn}{List from \code{\link[=build_dvn]{build_dvn()}} or \code{\link[=scrapeVarCross]{scrapeVarCross()}} with X and Y
indicators (\code{p1yvarnames}, \code{p2yvarnames}, \code{yindper} required).}

\item{lvxname, lvyname}{Base names for latent X and Y (partner suffixes from
\code{dvn$dist1} / \code{dvn$dist2} are appended, as in \code{\link[=scriptAPIM]{scriptAPIM()}}).}

\item{pop_values}{Named list; see \strong{Population values} below.}
}
\value{
Single string of lavaan model syntax.
}
\description{
Writes a fully specified population model with fixed numeric parameters for use
with \code{\link[lavaan:simulateData]{lavaan::simulateData()}}. Measurement blocks follow the same conventions
as \code{\link[=scriptPopCor]{scriptPopCor()}} for each construct; structural paths use a single actor
and partner coefficient on both partners' equations.
}
\section{Population values}{

\strong{X measurement} (lengths \code{nx = dvn$xindper}): \code{x_loadings_p1}, \code{x_loadings_p2}
(optional recycle), \code{x_residuals_p1}, \code{x_residuals_p2}, \code{x_coresids},
optional \code{x_intercepts_p1} / \code{x_intercepts_p2}.

\strong{Y measurement} (\code{ny = dvn$yindper}): \code{y_loadings_p1}, \code{y_loadings_p2},
\code{y_residuals_p1}, \code{y_residuals_p2}, \code{y_coresids}, optional \verb{y_intercepts_*}.

\strong{Latent X:} \code{x_lv_var} (scalar or length 2, default \code{c(1,1)}), \code{x_lv_cov}
(default 0), \code{x_lv_means} (optional, default \code{c(0,0)}).

\strong{Structural:} \code{actor}, \code{partner} (numeric), \code{y_lv_var} (scalar or length 2,
default \code{c(0.5, 0.5)}), \code{y_lv_cov} (default 0; covariance between latent Y
residuals).

Vectors of length 1 are recycled to \code{nx} or \code{ny} for measurement slots.
}

\seealso{
Other population-scripting functions: 
\code{\link{scriptPopCor}()},
\code{\link{scriptPopUni}()}
}
\concept{population-scripting functions}
