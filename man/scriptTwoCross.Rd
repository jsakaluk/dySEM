% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scriptTwoCross.R
\name{scriptTwoCross}
\alias{scriptTwoCross}
\title{A Function That Writes, Saves, and Exports Syntax for
Fitting Boutique Two-Construct Dyadic SEM Models}
\usage{
scriptTwoCross(
  dvn,
  x_scaleset = "FF",
  y_scaleset = "FF",
  lvxname = "X",
  lvyname = "Y",
  x_model = "cor",
  y_model = "cor",
  constr_dy_x_meas = c("loadings", "intercepts", "residuals"),
  constr_dy_x_struct = c("variances", "means"),
  constr_dy_y_meas = c("loadings", "intercepts", "residuals"),
  constr_dy_y_struct = c("variances", "means"),
  constr_dy_xy_struct = "free",
  includeMeanStruct = FALSE,
  writeTo = NULL,
  fileName = NULL
)
}
\arguments{
\item{dvn}{Input dvn list from \code{scrapeVarCross()} containing information for
both X and Y (i.e., must include both x_stem and y_stem arguments when
calling scrapeVarCross).}

\item{x_scaleset}{Character string specifying how to set the scale of latent X.
Options are \code{"FF"} (fixed-factor) or \code{"MV"} (marker variable). Default is \code{"FF"}.}

\item{y_scaleset}{Character string specifying how to set the scale of latent Y.
Options are \code{"FF"} (fixed-factor) or \code{"MV"} (marker variable). Default is \code{"FF"}.}

\item{lvxname}{Input character to (arbitrarily) name latent variable X in
\code{lavaan} syntax.}

\item{lvyname}{Input character to (arbitrarily) name latent variable Y in
\code{lavaan} syntax.}

\item{x_model}{Character string specifying the measurement model for X.
Options are \code{"uni"} (unidimensional), \code{"cor"} (correlated factors),
\code{"hier"} (hierarchical), or \code{"bifactor"}.}

\item{y_model}{Character string specifying the measurement model for Y.
Same options as \code{x_model}.}

\item{constr_dy_x_meas}{Input character vector detailing which measurement
model parameters to constrain across dyad members for latent X.}

\item{constr_dy_x_struct}{Input character vector detailing which structural
model parameters to constrain across dyad members for latent X.}

\item{constr_dy_y_meas}{Input character vector detailing which measurement
model parameters to constrain across dyad members for latent Y.}

\item{constr_dy_y_struct}{Input character vector detailing which structural
model parameters to constrain across dyad members for latent Y.}

\item{constr_dy_xy_struct}{Character string for the X-to-Y regression paths.
Options are \code{"free"} (default; all paths freely estimated) or \code{"zero"}
(all paths fixed to zero).}

\item{includeMeanStruct}{Logical for whether to include the mean structure
(intercepts and latent means) in the model. Defaults to \code{FALSE}.}

\item{writeTo}{A character string specifying a directory path to where the
output file(s) should be saved. If set to \code{"."}, the file(s) will be written
to the current working directory. The default is \code{NULL}, and examples use a
temporary directory created by \code{tempdir()}. When dealing with tabular output,
\code{writeTo} is only relevant if \code{gtTab = TRUE}.}

\item{fileName}{A character string specifying a desired base name for the
output file. The default is \code{NULL}. The specified name will be automatically
appended with the appropriate file extension (e.g., \code{.txt} for \code{lavaan}
scripts, \code{.rtf} for tabular output when \code{gtTab = TRUE}, or other extensions
as appropriate for the output type). If a file with the same name already
exists in the user's chosen directory, it will be overwritten.}
}
\value{
Character object of \code{lavaan} script that can be passed immediately to
\code{lavaan} functions.
}
\description{
This function takes the outputted object from \code{scrapeVarCross()} with both
X and Y indicators and automatically writes, returns, and exports (\code{.txt})
\code{lavaan} syntax for specifying a two-construct model where the measurement
structure of X and Y can differ (e.g., X as correlated factors, Y as
unidimensional).
}
\examples{
dvn <- scrapeVarCross(
  dat = commitmentQ,
  x_order = "spi", x_stem = "sat.g", x_delim1 = ".", x_delim2 = "_",
  distinguish_1 = "1", distinguish_2 = "2",
  y_order = "spi", y_stem = "com", y_delim1 = ".", y_delim2 = "_"
)

# Correlated X, unidimensional Y
scriptTwoCross(dvn, lvxname = "Sat", lvyname = "Com",
  x_model = "cor", y_model = "uni")

# Both correlated
scriptTwoCross(dvn, lvxname = "Sat", lvyname = "Com",
  x_model = "cor", y_model = "cor",
  writeTo = tempdir(), fileName = "twoCross_cor_cor")
}
\seealso{
\code{\link{scrapeVarCross}} which this function relies on;
\code{\link{scriptUni}}, \code{\link{scriptCor}}, \code{\link{scriptHier}},
\code{\link{scriptBifac}} for the underlying scripters;
\code{\link{scriptAPIM}} for a related two-construct function.

Other bi-construct script-writing functions: 
\code{\link{scriptAPIM}()},
\code{\link{scriptBiDy}()},
\code{\link{scriptCFM}()},
\code{\link{scriptMIM}()},
\code{\link{scriptObsAPIM}()}
}
\concept{bi-construct script-writing functions}
